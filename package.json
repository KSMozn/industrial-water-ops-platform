{
  "name": "industrial-water-ops-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Proof-of-concept operations platform for an industrial water treatment business. Demonstrates Postgres + Node + Vue + AI-agent patterns for the 24-week delivery brief.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:db": "docker compose up -d postgres",
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "seed": "npm run seed --workspace apps/api",
    "migrate": "npm run migrate --workspace apps/api",
    "test": "npm run test --workspace apps/api",
    "up": "docker compose up --build",
    "down": "docker compose down"
  }
}
